spring:
  application:
    name: mikuyun-admin-prod
  config:
    import:
      - nacos:${spring.application.name}?group=DEFAULT_GROUP&namespace=e0d7ece3-9ea3-4690-bf60-a9debdcc0c5a
  nacos:
    config:
      server-addr: mikuyun-nacos-standalone:8880
      file-extension: yaml